fn printf(str: char*, i: int) : int { return -1 }

fn printf(str: char*, i1: int, i2: int) : int { return -1 }

fn main()
{
    let printed_chars1 := printf("numbers: %d\n", 1)
    let printed_chars2 := printf("numbers: %d, %d\n", 1, 2)
    let printed_chars3 := printf("numbers: %d, %d, %d\n", 1, 2, 3) // tylko ta powinna używać printf z C
}