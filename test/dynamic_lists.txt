extern fn printf(str: char*, variadic) : int

const static_array_length = 12

struct dynamics 
{
    list: int[],
    array: int[static_array_length]
}

fn main () 
{ 
    let x := new int[]
    
    x.add(1)
    x.add(2)
    x.add(3)

    let len := x.length()
    let cap := x.capacity()

    if (x.add(4))
    {
        printf("\ntest if")
    }

    for (let x:= new int[]; x.capacity() < 5; x.add(2))
    {
        printf("\ntest for")
    }

    delete x

    let y := auto int[]

    y[0] = 1
    y[1] = 2 
    y[2] = 3

     printf("\nkoniec testu")
}